package com.PIPPIP5789.betterwithfantasy.blocks;

import minefantasy.mfr.tile.TileEntityForge;
import net.minecraft.tileentity.TileEntity;
import net.minecraftforge.fml.common.eventhandler.SubscribeEvent;

public class PumpBellows {

    /*@SubscribeEvent
    public static void pumpBellows() {
        if (!(this.fuel <= 0.0F)) {
            TileEntity tile = this.world.getTileEntity(this.pos.add(x, 0, z));
            if (tile != null) {
                if (tile instanceof TileEntityForge) {
                    TileEntityForge forge = (TileEntityForge)tile;
                    forge.onUsedWithBellows(pump);
                }

            }
        }
    }*/

}
